machine:
  node:
    version: 6.2.0

dependencies:
  pre:
    - git checkout -f $CIRCLE_BRANCH
    - git fetch origin master
    - sh bin/list-env-versions

test:
  override:
    - npm run test-ci-build
  post:
    - time npm run vile
