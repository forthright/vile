machine:
  node:
    version: 5.10.1

dependencies:
  pre:
    - sh bin/check-libstdc-deb
    - npm install -g npm
    - sh bin/list-env-versions

test:
  override:
    - npm run test-ci-build
  post:
    - time npm run vile
