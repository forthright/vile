machine:
  node:
    version: 5.10.1

checkout:
  pre:
    - rm -rf .git

dependencies:
  pre:
    - npm install -g npm
    - uname -a
    - node --version
    - npm --version

test:
  override:
    - npm run test-ci-build
  post:
    - npm run vile
