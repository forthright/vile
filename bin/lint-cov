#!/usr/bin/env node
require("shelljs/global")

exec(
  "npm run istanbul -- " +
    "check-coverage coverage/coverage.json " +
      "--statements 90 " +
      "--functions 90 " +
      "--branches 90 " +
      "--lines 90",
  function (code) {
    if (code !== 0) exit(code)
  })
