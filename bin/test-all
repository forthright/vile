#!/usr/bin/env node
require("shelljs/global")

exec(
  "npm run -s test-non-system",
  function (code) {
    if (code !== 0) exit(code)

    exec(
      "npm run -s test-system",
      function (code) {
        if (code !== 0) exit(code)
      })
  })
