#!/usr/bin/env node
require("shelljs/global")

exec(
  "npm run -s tsc",
  function(code, stdout, stderr) {
    if (code !== 0) exit(code)

    exec(
      "npm run -s babel -- -d lib -m common .build",
      function(code, stdout, stderr) {
        if (code !== 0) exit(code)
      })
  })

