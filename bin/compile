#!/usr/bin/env node
require("shelljs/global")

exec(
  "npm run tsc",
  function(code, stdout, stderr) {
    if (code !== 0) exit(code)

    exec(
      "npm run babel -- -d lib -m common .build",
      function(code, stdout, stderr) {
        if (code !== 0) exit(code)
      })
  })

