var Bluebird = require("bluebird")

function punish(config) {
  return new Bluebird(function (resolve, reject) {
    setTimeout(() => {
      resolve([
        { type: "warning", message: "test", signature: "async" },
        { type: "test", message: "test", signature: "async" }
      ])
    }, 250)
  })
}

module.exports = {
  punish: punish
}
