var Bluebird = require("bluebird")

function punish(config) {
  return new Bluebird(function (resolve, reject) {
    resolve([
      { type: "test", signature: "async" }
    ])
  })
}

module.exports = {
  punish: punish
}
